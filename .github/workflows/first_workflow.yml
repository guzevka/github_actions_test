name: First Workflow

on: push

jobs: 
  run-first-command: 
    runs-on: ubuntu-latest
    steps:
      - name: Display text
        run: echo "Some text"
      - name: Command in Multiline
        run: |
          python --version
          docker version
